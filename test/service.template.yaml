apiVersion: v1
kind: Service

metadata:
  name: {{name}}
  labels:
    app: {{name}}

spec:
  type: LoadBalancer
  selector:
    app: {{name}}
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 3000
